# hello-bin

Exemplo **generic** do registry XGIT (`xcorp/hello-bin`) — um tarball com
ficheiros soltos (não npm, não PyPI). Útil para binários, schemas ou assets.

Download autenticado em `xgit.corp` (aba Packages do repo `xcorp/hello-bin`).
