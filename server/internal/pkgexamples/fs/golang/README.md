# hello-go

Exemplo **Go** no XGIT (`xcorp/hello-go`). O registry desta fatia é generic
(tarball) — GOPROXY próprio fica no backlog. Clone na VPN:

```sh
git clone https://xgit.corp.ihuull.com/xcorp/hello-go
go test ./...
```
