[project]
name = "hello-ihuull"
version = "0.1.0"
description = "Exemplo PyPI no registry XGIT (xgit.corp)."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
