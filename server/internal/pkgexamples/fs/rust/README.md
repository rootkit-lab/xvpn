# hello-rs

Exemplo **Rust** no XGIT (`xcorp/hello-rs`). Publicado como tarball **generic**
(crates.io próprio não entra nesta fatia). Clone na VPN:

```sh
git clone https://xgit.corp.ihuull.com/xcorp/hello-rs
cargo test
```
