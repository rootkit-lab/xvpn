# ihuull.codespace

Extensão bakeada na imagem `ihuull/codespace`. Chat e **Generate commit message** no Source Control.

O proxy é o `xvpn-server` (`POST /api/xcodespaces/llm/*`) no host `cs-*`. Provedor e key ficam em **xadmin → Settings**. Sem Copilot, sem Continue, sem `docker.sock`.
