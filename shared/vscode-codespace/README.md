# ihuull.codespace

Extensão bakeada na imagem `ihuull/codespace`. **Chat** à direita em `viewsContainers.secondarySideBar` (a imagem aplica `patch-auxiliary-bar.js` no OpenVSCode 1.98, que só vinha com `activitybar`/`panel`) e **Generate commit message** no Source Control. Edições pedem **Aplicar** / **Sempre** / **Recusar**; **Sempre** (ou o toggle Auto) grava `ihuull.codespace.autoApply` e deixa de perguntar em `write_file`/`apply_patch`. Terminal e MCP extra continuam a confirmar.

O proxy é o `xvpn-server` (`GET /api/xcodespaces/llm/models`, `POST /api/xcodespaces/llm/*`). A extensão é `extensionKind: workspace` (Node): usa URL absoluta `https://cs-<id>.corp.ihuull.com` e o token Git de `.git/xvpn-credentials`. Chrome: modos Agent / Ask / Debug / Plan e seletor de modelo (catálogo do provedor no xadmin). Timeline: Thinking, resumo de exploração e cards de tool (não dump `tool N…`). Composer: `@arquivo`, `#git`/`#docs`/`#pasta`, `$term`, `/comando`. Terminal espera o stdout (`python3` + `env:{KEY:valor}`, sem shell). MCP stdio: think, memory, docs. Logs em `.cursor/agent/` (ou `/tmp/xcs-agent`); Review + Stop. Workspace = clone do `xgit.corp`, não GitHub. Grava `user.name`/`user.email` do dono no clone (Source Control). Lê `AGENTS.md` (ou contrato ihuull), `CONTRIBUTING.md`, `.cursor/skills` e `.cursor/rules`. Tools (read/glob/analyze/edit/term) só no clone, com confirmação. Loop até 24 turns; no teto resume o que já leu. Provedor e key ficam em **xadmin → Settings**. Sem Copilot, sem Continue, sem `docker.sock`.
