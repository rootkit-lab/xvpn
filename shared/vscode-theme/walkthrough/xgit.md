# XGIT e intranet

| O quê | Onde |
|---|---|
| Repositório | `https://xgit.corp.ihuull.com/<slug>` |
| Console | `xadmin.corp` |
| Catálogo de codespaces | `xcodespaces.corp` |
| Este IDE | `cs-<id>.corp.ihuull.com` |

Tudo isso só resolve no DNS interno (`10.66.66.1`). Fora da VPN o host não abre.

Chat ihuull, generate commit e ENVs do projeto no XGIT entram nas próximas entregas da Fase 51 — sem Copilot, sem Continue, sem `docker.sock`.
